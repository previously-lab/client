# Keep @previously-lab/kernel exempt from pnpm 11's default 24h
# minimumReleaseAge: the kernel is published minutes before the client release
# by our own CI (with provenance), so the first-party dependency would always
# fail the freshness gate at release time. Everything else stays gated.
minimumReleaseAgeExclude:
  - '@previously-lab/kernel'
