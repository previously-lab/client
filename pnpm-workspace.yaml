# TEMPORARY (pre-release): @previously-lab/kernel@0.9.0 is not on npm yet, so
# its resolution is pinned to the locally built kernel package. Remove this
# file's `overrides` entry once @previously-lab/kernel is published. (pnpm 11
# reads overrides from this file, not from package.json.)
overrides:
  "@previously-lab/kernel": link:../Aftrbrez/dist-kernel
